@q ** (2) Flatten.  @>
@ Flatten.  
\initials{LDF 2022.05.02.}

\LOG
\initials{LDF 2022.05.02.}
Added this function.
\ENDLOG

@<Declare |Picture| functions@>=
int
flatten(int coord = 2, real r = 0.0);

@q *** (3) Definition @>
@
@<Define |Picture| functions@>=
int
Picture::flatten(int coord, real r)
{
@q **** (4) @>

#if DEBUG_COMPILE
   bool DEBUG = true; /* |false| */ 
   if (DEBUG)
   { 
      cerr << "Entering `Picture::flatten'." << endl;
   }  
#endif /* |DEBUG_COMPILE|  */@; 

@q **** (4) @>

   for (vector<Shape*>::iterator iter = shapes.begin();
        iter != shapes.end();
        ++iter)
   {
       (*iter)->flatten(coord, r);
   }

   for (vector<Label*>::iterator iter = labels.begin();
        iter != labels.end();
        ++iter)
   {
       (*iter)->pt->flatten(coord, r);
   }


@q **** (4) @>

#if DEBUG_COMPILE
   if (DEBUG)
   { 
      cerr << "Exiting `Picture::flatten' successfully with return value 0." 
           << endl;
   }  
#endif /* |DEBUG_COMPILE|  */@; 

   return 0;

} /* End of |Picture::flatten| definition  */
